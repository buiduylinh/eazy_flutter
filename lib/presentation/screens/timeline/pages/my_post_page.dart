import 'package:flutter/material.dart';

class MyPostPage extends StatelessWidget {
  MyPostPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("MyPost Page"),);
  }
}
